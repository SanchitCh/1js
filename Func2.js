function name() {
  var a=2;
  console.log(a);
}
name();
console.log(a);

// var a is accessible only inside the function and cannot be accessed outside the function.
// Hence it is a local scope.
